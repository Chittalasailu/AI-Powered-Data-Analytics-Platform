# Query optimization notes

Generated by `src/transformation/sql_runner.py --compare-optimizations --write-notes`.
Each section below runs the *same* result set two different ways and compares
their real `EXPLAIN FORMATTED` physical plans and a single local wall-clock
timing, captured directly from this Spark session -- nothing here is
hand-typed or assumed.

---

## regional_join_strategy

Joining `transactions` to the 5-row `region_dim` table: forced sort-merge join vs. forced broadcast join.

### Unoptimized -- /*+ MERGE(t, d) */ forces a shuffle (sort-merge) join

```sql
SELECT /*+ MERGE(t, d) */
                t.region, d.sales_territory, ROUND(SUM(t.total_amount), 2) AS revenue
            FROM transactions t
            JOIN region_dim d ON t.region = d.region
            GROUP BY t.region, d.sales_territory
```

Rows returned: **5** | Wall-clock (single local run): **11.60s**

<details>
<summary>EXPLAIN FORMATTED (unoptimized)</summary>

```
== Physical Plan ==
AdaptiveSparkPlan (12)
+- HashAggregate (11)
   +- HashAggregate (10)
      +- Project (9)
         +- SortMergeJoin Inner (8)
            :- Sort (4)
            :  +- Exchange (3)
            :     +- Filter (2)
            :        +- Scan parquet  (1)
            +- Sort (7)
               +- Exchange (6)
                  +- Scan ExistingRDD (5)


(1) Scan parquet 
Output [2]: [total_amount#7, region#8]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(region)]
ReadSchema: struct<total_amount:double,region:string>

(2) Filter
Input [2]: [total_amount#7, region#8]
Condition : isnotnull(region#8)

(3) Exchange
Input [2]: [total_amount#7, region#8]
Arguments: hashpartitioning(region#8, 200), ENSURE_REQUIREMENTS, [plan_id=53]

(4) Sort
Input [2]: [total_amount#7, region#8]
Arguments: [region#8 ASC NULLS FIRST], false, 0

(5) Scan ExistingRDD
Output [2]: [region#12, sales_territory#13]
Arguments: [region#12, sales_territory#13], MapPartitionsRDD[6] at applySchemaToPythonRDD at DirectMethodHandleAccessor.java:104, ExistingRDD, UnknownPartitioning(0)

(6) Exchange
Input [2]: [region#12, sales_territory#13]
Arguments: hashpartitioning(region#12, 200), ENSURE_REQUIREMENTS, [plan_id=54]

(7) Sort
Input [2]: [region#12, sales_territory#13]
Arguments: [region#12 ASC NULLS FIRST], false, 0

(8) SortMergeJoin
Left keys [1]: [region#8]
Right keys [1]: [region#12]
Join type: Inner
Join condition: None

(9) Project
Output [3]: [total_amount#7, region#8, sales_territory#13]
Input [4]: [total_amount#7, region#8, region#12, sales_territory#13]

(10) HashAggregate
Input [3]: [total_amount#7, region#8, sales_territory#13]
Keys [2]: [region#8, sales_territory#13]
Functions [1]: [partial_sum(total_amount#7)]
Aggregate Attributes [1]: [sum#54]
Results [3]: [region#8, sales_territory#13, sum#55]

(11) HashAggregate
Input [3]: [region#8, sales_territory#13, sum#55]
Keys [2]: [region#8, sales_territory#13]
Functions [1]: [sum(total_amount#7)]
Aggregate Attributes [1]: [sum(total_amount#7)#53]
Results [3]: [region#8, sales_territory#13, round(sum(total_amount#7)#53, 2) AS revenue#52]

(12) AdaptiveSparkPlan
Output [3]: [region#8, sales_territory#13, revenue#52]
Arguments: isFinalPlan=false
```

</details>

### Optimized -- /*+ BROADCAST(d) */ forces a broadcast hash join

```sql
SELECT /*+ BROADCAST(d) */
                t.region, d.sales_territory, ROUND(SUM(t.total_amount), 2) AS revenue
            FROM transactions t
            JOIN region_dim d ON t.region = d.region
            GROUP BY t.region, d.sales_territory
```

Rows returned: **5** | Wall-clock (single local run): **9.44s**

<details>
<summary>EXPLAIN FORMATTED (optimized)</summary>

```
== Physical Plan ==
AdaptiveSparkPlan (10)
+- HashAggregate (9)
   +- Exchange (8)
      +- HashAggregate (7)
         +- Project (6)
            +- BroadcastHashJoin Inner BuildRight (5)
               :- Filter (2)
               :  +- Scan parquet  (1)
               +- BroadcastExchange (4)
                  +- Scan ExistingRDD (3)


(1) Scan parquet 
Output [2]: [total_amount#7, region#8]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(region)]
ReadSchema: struct<total_amount:double,region:string>

(2) Filter
Input [2]: [total_amount#7, region#8]
Condition : isnotnull(region#8)

(3) Scan ExistingRDD
Output [2]: [region#12, sales_territory#13]
Arguments: [region#12, sales_territory#13], MapPartitionsRDD[6] at applySchemaToPythonRDD at DirectMethodHandleAccessor.java:104, ExistingRDD, UnknownPartitioning(0)

(4) BroadcastExchange
Input [2]: [region#12, sales_territory#13]
Arguments: HashedRelationBroadcastMode(List(input[0, string, false]),false), [plan_id=331]

(5) BroadcastHashJoin
Left keys [1]: [region#8]
Right keys [1]: [region#12]
Join type: Inner
Join condition: None

(6) Project
Output [3]: [total_amount#7, region#8, sales_territory#13]
Input [4]: [total_amount#7, region#8, region#12, sales_territory#13]

(7) HashAggregate
Input [3]: [total_amount#7, region#8, sales_territory#13]
Keys [2]: [region#8, sales_territory#13]
Functions [1]: [partial_sum(total_amount#7)]
Aggregate Attributes [1]: [sum#65]
Results [3]: [region#8, sales_territory#13, sum#66]

(8) Exchange
Input [3]: [region#8, sales_territory#13, sum#66]
Arguments: hashpartitioning(region#8, sales_territory#13, 200), ENSURE_REQUIREMENTS, [plan_id=336]

(9) HashAggregate
Input [3]: [region#8, sales_territory#13, sum#66]
Keys [2]: [region#8, sales_territory#13]
Functions [1]: [sum(total_amount#7)]
Aggregate Attributes [1]: [sum(total_amount#7)#64]
Results [3]: [region#8, sales_territory#13, round(sum(total_amount#7)#64, 2) AS revenue#63]

(10) AdaptiveSparkPlan
Output [3]: [region#8, sales_territory#13, revenue#63]
Arguments: isFinalPlan=false
```

</details>

### Why

`region_dim` is 5 rows. A sort-merge join still shuffles the *entire* `transactions` table across the network to co-locate matching keys before it can join anything, purely to attach a lookup value with 5 possible outcomes. A broadcast join ships one side to every executor once and joins locally -- no shuffle of the larger side at all. Worth checking rather than assuming: with *no* hint at all on this small local dataset, Spark's cost-based optimizer does pick a broadcast join here too -- but it broadcasts `transactions` (`BuildLeft`), not `region_dim`. `region_dim` comes from `spark.createDataFrame()` on driver-side Python data (a `Scan ExistingRDD`), which doesn't carry the file-based byte-size statistics that the parquet-backed `transactions` scan does, so the optimizer trusts the known size of `transactions` more than its guess at `region_dim`'s size -- and on this small test dataset, `transactions` itself is small enough to broadcast, so it works out fine by coincidence. At real fact-table scale that coincidence disappears: the same missing-statistics gap could just as easily make the optimizer fall back to a sort-merge join instead of broadcasting the tiny dimension table it should. `/*+ BROADCAST(d) */` pins down which side gets broadcast regardless of which side's statistics the optimizer happens to trust.


---

## ltv_ranking_self_join_vs_window

Ranking customers by lifetime value: a self-join anti-pattern vs. a window function.

### Unoptimized -- self-join on an inequality to emulate RANK()

```sql
WITH customer_totals AS (
                SELECT customer_id, SUM(total_amount) AS lifetime_value
                FROM transactions
                WHERE customer_id != 'UNKNOWN_CUSTOMER'
                GROUP BY customer_id
            )
            SELECT
                a.customer_id,
                a.lifetime_value,
                COUNT(b.customer_id) + 1 AS ltv_rank
            FROM customer_totals a
            LEFT JOIN customer_totals b ON b.lifetime_value > a.lifetime_value
            GROUP BY a.customer_id, a.lifetime_value
```

Rows returned: **5997** | Wall-clock (single local run): **1.32s**

<details>
<summary>EXPLAIN FORMATTED (unoptimized)</summary>

```
== Physical Plan ==
AdaptiveSparkPlan (17)
+- HashAggregate (16)
   +- HashAggregate (15)
      +- Project (14)
         +- BroadcastNestedLoopJoin LeftOuter BuildRight (13)
            :- HashAggregate (5)
            :  +- Exchange (4)
            :     +- HashAggregate (3)
            :        +- Filter (2)
            :           +- Scan parquet  (1)
            +- BroadcastExchange (12)
               +- Filter (11)
                  +- HashAggregate (10)
                     +- Exchange (9)
                        +- HashAggregate (8)
                           +- Filter (7)
                              +- Scan parquet  (6)


(1) Scan parquet 
Output [2]: [customer_id#1, total_amount#7]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(customer_id), Not(EqualTo(customer_id,UNKNOWN_CUSTOMER))]
ReadSchema: struct<customer_id:string,total_amount:double>

(2) Filter
Input [2]: [customer_id#1, total_amount#7]
Condition : (isnotnull(customer_id#1) AND NOT (customer_id#1 = UNKNOWN_CUSTOMER))

(3) HashAggregate
Input [2]: [customer_id#1, total_amount#7]
Keys [1]: [customer_id#1]
Functions [1]: [partial_sum(total_amount#7)]
Aggregate Attributes [1]: [sum#108]
Results [2]: [customer_id#1, sum#109]

(4) Exchange
Input [2]: [customer_id#1, sum#109]
Arguments: hashpartitioning(customer_id#1, 200), ENSURE_REQUIREMENTS, [plan_id=597]

(5) HashAggregate
Input [2]: [customer_id#1, sum#109]
Keys [1]: [customer_id#1]
Functions [1]: [sum(total_amount#7)]
Aggregate Attributes [1]: [sum(total_amount#7)#76]
Results [2]: [customer_id#1, sum(total_amount#7)#76 AS lifetime_value#75]

(6) Scan parquet 
Output [2]: [customer_id#94, total_amount#100]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(customer_id), Not(EqualTo(customer_id,UNKNOWN_CUSTOMER))]
ReadSchema: struct<customer_id:string,total_amount:double>

(7) Filter
Input [2]: [customer_id#94, total_amount#100]
Condition : (isnotnull(customer_id#94) AND NOT (customer_id#94 = UNKNOWN_CUSTOMER))

(8) HashAggregate
Input [2]: [customer_id#94, total_amount#100]
Keys [1]: [customer_id#94]
Functions [1]: [partial_sum(total_amount#100)]
Aggregate Attributes [1]: [sum#110]
Results [2]: [customer_id#94, sum#111]

(9) Exchange
Input [2]: [customer_id#94, sum#111]
Arguments: hashpartitioning(customer_id#94, 200), ENSURE_REQUIREMENTS, [plan_id=599]

(10) HashAggregate
Input [2]: [customer_id#94, sum#111]
Keys [1]: [customer_id#94]
Functions [1]: [sum(total_amount#100)]
Aggregate Attributes [1]: [sum(total_amount#100)#76]
Results [2]: [customer_id#94, sum(total_amount#100)#76 AS lifetime_value#78]

(11) Filter
Input [2]: [customer_id#94, lifetime_value#78]
Condition : isnotnull(lifetime_value#78)

(12) BroadcastExchange
Input [2]: [customer_id#94, lifetime_value#78]
Arguments: IdentityBroadcastMode, [plan_id=603]

(13) BroadcastNestedLoopJoin
Join type: LeftOuter
Join condition: (lifetime_value#78 > lifetime_value#75)

(14) Project
Output [3]: [customer_id#1, lifetime_value#75, customer_id#94]
Input [4]: [customer_id#1, lifetime_value#75, customer_id#94, lifetime_value#78]

(15) HashAggregate
Input [3]: [customer_id#1, lifetime_value#75, customer_id#94]
Keys [2]: [customer_id#1, knownfloatingpointnormalized(normalizenanandzero(lifetime_value#75)) AS lifetime_value#75]
Functions [1]: [partial_count(customer_id#94)]
Aggregate Attributes [1]: [count#106L]
Results [3]: [customer_id#1, lifetime_value#75, count#107L]

(16) HashAggregate
Input [3]: [customer_id#1, lifetime_value#75, count#107L]
Keys [2]: [customer_id#1, lifetime_value#75]
Functions [1]: [count(customer_id#94)]
Aggregate Attributes [1]: [count(customer_id#94)#79L]
Results [3]: [customer_id#1, lifetime_value#75, (count(customer_id#94)#79L + 1) AS ltv_rank#74L]

(17) AdaptiveSparkPlan
Output [3]: [customer_id#1, lifetime_value#75, ltv_rank#74L]
Arguments: isFinalPlan=false
```

</details>

### Optimized -- RANK() OVER (ORDER BY lifetime_value DESC)

```sql
WITH customer_totals AS (
                SELECT customer_id, SUM(total_amount) AS lifetime_value
                FROM transactions
                WHERE customer_id != 'UNKNOWN_CUSTOMER'
                GROUP BY customer_id
            )
            SELECT
                customer_id,
                lifetime_value,
                RANK() OVER (ORDER BY lifetime_value DESC) AS ltv_rank
            FROM customer_totals
```

Rows returned: **5997** | Wall-clock (single local run): **1.31s**

<details>
<summary>EXPLAIN FORMATTED (optimized)</summary>

```
== Physical Plan ==
AdaptiveSparkPlan (9)
+- Window (8)
   +- Sort (7)
      +- Exchange (6)
         +- HashAggregate (5)
            +- Exchange (4)
               +- HashAggregate (3)
                  +- Filter (2)
                     +- Scan parquet  (1)


(1) Scan parquet 
Output [2]: [customer_id#1, total_amount#7]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(customer_id), Not(EqualTo(customer_id,UNKNOWN_CUSTOMER))]
ReadSchema: struct<customer_id:string,total_amount:double>

(2) Filter
Input [2]: [customer_id#1, total_amount#7]
Condition : (isnotnull(customer_id#1) AND NOT (customer_id#1 = UNKNOWN_CUSTOMER))

(3) HashAggregate
Input [2]: [customer_id#1, total_amount#7]
Keys [1]: [customer_id#1]
Functions [1]: [partial_sum(total_amount#7)]
Aggregate Attributes [1]: [sum#156]
Results [2]: [customer_id#1, sum#157]

(4) Exchange
Input [2]: [customer_id#1, sum#157]
Arguments: hashpartitioning(customer_id#1, 200), ENSURE_REQUIREMENTS, [plan_id=735]

(5) HashAggregate
Input [2]: [customer_id#1, sum#157]
Keys [1]: [customer_id#1]
Functions [1]: [sum(total_amount#7)]
Aggregate Attributes [1]: [sum(total_amount#7)#147]
Results [2]: [customer_id#1, sum(total_amount#7)#147 AS lifetime_value#146]

(6) Exchange
Input [2]: [customer_id#1, lifetime_value#146]
Arguments: SinglePartition, ENSURE_REQUIREMENTS, [plan_id=738]

(7) Sort
Input [2]: [customer_id#1, lifetime_value#146]
Arguments: [lifetime_value#146 DESC NULLS LAST], false, 0

(8) Window
Input [2]: [customer_id#1, lifetime_value#146]
Arguments: [rank(lifetime_value#146) windowspecdefinition(lifetime_value#146 DESC NULLS LAST, specifiedwindowframe(RowFrame, unboundedpreceding$(), currentrow$())) AS ltv_rank#145], [lifetime_value#146 DESC NULLS LAST]

(9) AdaptiveSparkPlan
Output [3]: [customer_id#1, lifetime_value#146, ltv_rank#145]
Arguments: isFinalPlan=false
```

</details>

### Why

The self-join computes each customer's rank by counting how many other customers out-earned them -- an inequality join, so Spark can't use a hash join at all; it falls back to a nested-loop-style join comparing every customer against every other customer (~n^2 comparisons -- tens of millions of comparisons for a few thousand customers). `RANK() OVER (...)` computes the identical ranking with a single sort pass (n log n) and no join whatsoever. This is a textbook case where reaching for a join out of habit is dramatically more expensive than the window-function primitive SQL already provides for exactly this problem.


---

## cohort_prefilter_before_self_join

Cohort retention's self-join of `transactions` against itself: unfiltered vs. filtering the sentinel customer and null dates first.

### Unoptimized -- no filter -- UNKNOWN_CUSTOMER and null-date rows flow into both join branches

```sql
WITH first_purchase AS (
                SELECT customer_id, date_trunc('month', MIN(purchase_date)) AS cohort_month
                FROM transactions
                GROUP BY customer_id
            ),
            monthly_activity AS (
                SELECT DISTINCT customer_id, date_trunc('month', purchase_date) AS activity_month
                FROM transactions
                WHERE purchase_date IS NOT NULL
            )
            SELECT f.cohort_month, COUNT(DISTINCT a.customer_id) AS active_customers
            FROM first_purchase f
            JOIN monthly_activity a ON f.customer_id = a.customer_id
            GROUP BY f.cohort_month
```

Rows returned: **28** | Wall-clock (single local run): **2.88s**

<details>
<summary>EXPLAIN FORMATTED (unoptimized)</summary>

```
== Physical Plan ==
AdaptiveSparkPlan (20)
+- HashAggregate (19)
   +- Exchange (18)
      +- HashAggregate (17)
         +- HashAggregate (16)
            +- HashAggregate (15)
               +- Project (14)
                  +- BroadcastHashJoin Inner BuildRight (13)
                     :- HashAggregate (5)
                     :  +- Exchange (4)
                     :     +- HashAggregate (3)
                     :        +- Filter (2)
                     :           +- Scan parquet  (1)
                     +- BroadcastExchange (12)
                        +- HashAggregate (11)
                           +- Exchange (10)
                              +- HashAggregate (9)
                                 +- Project (8)
                                    +- Filter (7)
                                       +- Scan parquet  (6)


(1) Scan parquet 
Output [2]: [customer_id#1, purchase_date#4]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(customer_id)]
ReadSchema: struct<customer_id:string,purchase_date:date>

(2) Filter
Input [2]: [customer_id#1, purchase_date#4]
Condition : isnotnull(customer_id#1)

(3) HashAggregate
Input [2]: [customer_id#1, purchase_date#4]
Keys [1]: [customer_id#1]
Functions [1]: [partial_min(purchase_date#4)]
Aggregate Attributes [1]: [min#186]
Results [2]: [customer_id#1, min#187]

(4) Exchange
Input [2]: [customer_id#1, min#187]
Arguments: hashpartitioning(customer_id#1, 200), ENSURE_REQUIREMENTS, [plan_id=905]

(5) HashAggregate
Input [2]: [customer_id#1, min#187]
Keys [1]: [customer_id#1]
Functions [1]: [min(purchase_date#4)]
Aggregate Attributes [1]: [min(purchase_date#4)#180]
Results [2]: [customer_id#1, date_trunc(month, cast(min(purchase_date#4)#180 as timestamp), Some(Asia/Calcutta)) AS cohort_month#166]

(6) Scan parquet 
Output [2]: [customer_id#169, purchase_date#172]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(purchase_date), IsNotNull(customer_id)]
ReadSchema: struct<customer_id:string,purchase_date:date>

(7) Filter
Input [2]: [customer_id#169, purchase_date#172]
Condition : (isnotnull(purchase_date#172) AND isnotnull(customer_id#169))

(8) Project
Output [2]: [customer_id#169, date_trunc(month, cast(purchase_date#172 as timestamp), Some(Asia/Calcutta)) AS activity_month#167]
Input [2]: [customer_id#169, purchase_date#172]

(9) HashAggregate
Input [2]: [customer_id#169, activity_month#167]
Keys [2]: [customer_id#169, activity_month#167]
Functions: []
Aggregate Attributes: []
Results [2]: [customer_id#169, activity_month#167]

(10) Exchange
Input [2]: [customer_id#169, activity_month#167]
Arguments: hashpartitioning(customer_id#169, activity_month#167, 200), ENSURE_REQUIREMENTS, [plan_id=907]

(11) HashAggregate
Input [2]: [customer_id#169, activity_month#167]
Keys [2]: [customer_id#169, activity_month#167]
Functions: []
Aggregate Attributes: []
Results [1]: [customer_id#169]

(12) BroadcastExchange
Input [1]: [customer_id#169]
Arguments: HashedRelationBroadcastMode(List(input[0, string, true]),false), [plan_id=910]

(13) BroadcastHashJoin
Left keys [1]: [customer_id#1]
Right keys [1]: [customer_id#169]
Join type: Inner
Join condition: None

(14) Project
Output [2]: [cohort_month#166, customer_id#169]
Input [3]: [customer_id#1, cohort_month#166, customer_id#169]

(15) HashAggregate
Input [2]: [cohort_month#166, customer_id#169]
Keys [2]: [cohort_month#166, customer_id#169]
Functions: []
Aggregate Attributes: []
Results [2]: [cohort_month#166, customer_id#169]

(16) HashAggregate
Input [2]: [cohort_month#166, customer_id#169]
Keys [2]: [cohort_month#166, customer_id#169]
Functions: []
Aggregate Attributes: []
Results [2]: [cohort_month#166, customer_id#169]

(17) HashAggregate
Input [2]: [cohort_month#166, customer_id#169]
Keys [1]: [cohort_month#166]
Functions [1]: [partial_count(distinct customer_id#169)]
Aggregate Attributes [1]: [count(customer_id#169)#181L]
Results [2]: [cohort_month#166, count#184L]

(18) Exchange
Input [2]: [cohort_month#166, count#184L]
Arguments: hashpartitioning(cohort_month#166, 200), ENSURE_REQUIREMENTS, [plan_id=917]

(19) HashAggregate
Input [2]: [cohort_month#166, count#184L]
Keys [1]: [cohort_month#166]
Functions [1]: [count(distinct customer_id#169)]
Aggregate Attributes [1]: [count(customer_id#169)#181L]
Results [2]: [cohort_month#166, count(customer_id#169)#181L AS active_customers#165L]

(20) AdaptiveSparkPlan
Output [2]: [cohort_month#166, active_customers#165L]
Arguments: isFinalPlan=false
```

</details>

### Optimized -- shared valid_transactions CTE filters both branches before the join

```sql
WITH valid_transactions AS (
                SELECT customer_id, purchase_date
                FROM transactions
                WHERE customer_id != 'UNKNOWN_CUSTOMER' AND purchase_date IS NOT NULL
            ),
            first_purchase AS (
                SELECT customer_id, date_trunc('month', MIN(purchase_date)) AS cohort_month
                FROM valid_transactions
                GROUP BY customer_id
            ),
            monthly_activity AS (
                SELECT DISTINCT customer_id, date_trunc('month', purchase_date) AS activity_month
                FROM valid_transactions
            )
            SELECT f.cohort_month, COUNT(DISTINCT a.customer_id) AS active_customers
            FROM first_purchase f
            JOIN monthly_activity a ON f.customer_id = a.customer_id
            GROUP BY f.cohort_month
```

Rows returned: **28** | Wall-clock (single local run): **1.83s**

<details>
<summary>EXPLAIN FORMATTED (optimized)</summary>

```
== Physical Plan ==
AdaptiveSparkPlan (20)
+- HashAggregate (19)
   +- Exchange (18)
      +- HashAggregate (17)
         +- HashAggregate (16)
            +- HashAggregate (15)
               +- Project (14)
                  +- BroadcastHashJoin Inner BuildRight (13)
                     :- HashAggregate (5)
                     :  +- Exchange (4)
                     :     +- HashAggregate (3)
                     :        +- Filter (2)
                     :           +- Scan parquet  (1)
                     +- BroadcastExchange (12)
                        +- HashAggregate (11)
                           +- Exchange (10)
                              +- HashAggregate (9)
                                 +- Project (8)
                                    +- Filter (7)
                                       +- Scan parquet  (6)


(1) Scan parquet 
Output [2]: [customer_id#262, purchase_date#265]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(customer_id), Not(EqualTo(customer_id,UNKNOWN_CUSTOMER)), IsNotNull(purchase_date)]
ReadSchema: struct<customer_id:string,purchase_date:date>

(2) Filter
Input [2]: [customer_id#262, purchase_date#265]
Condition : ((isnotnull(customer_id#262) AND NOT (customer_id#262 = UNKNOWN_CUSTOMER)) AND isnotnull(purchase_date#265))

(3) HashAggregate
Input [2]: [customer_id#262, purchase_date#265]
Keys [1]: [customer_id#262]
Functions [1]: [partial_min(purchase_date#265)]
Aggregate Attributes [1]: [min#293]
Results [2]: [customer_id#262, min#294]

(4) Exchange
Input [2]: [customer_id#262, min#294]
Arguments: hashpartitioning(customer_id#262, 200), ENSURE_REQUIREMENTS, [plan_id=1351]

(5) HashAggregate
Input [2]: [customer_id#262, min#294]
Keys [1]: [customer_id#262]
Functions [1]: [min(purchase_date#265)]
Aggregate Attributes [1]: [min(purchase_date#265)#203]
Results [2]: [customer_id#262, date_trunc(month, cast(min(purchase_date#265)#203 as timestamp), Some(Asia/Calcutta)) AS cohort_month#205]

(6) Scan parquet 
Output [2]: [customer_id#275, purchase_date#278]
Batched: true
Location: InMemoryFileIndex [file:/C:/Users/NAVEEN/AppData/Local/Temp/claude/c--Users-NAVEEN-Downloads-AI-Powered-Data-Analytics-Platform/8a7ee91b-34f2-490a-8a3f-b83973103a1e/scratchpad/testrun/data/processed/transactions_cleaned]
PushedFilters: [IsNotNull(customer_id), Not(EqualTo(customer_id,UNKNOWN_CUSTOMER)), IsNotNull(purchase_date)]
ReadSchema: struct<customer_id:string,purchase_date:date>

(7) Filter
Input [2]: [customer_id#275, purchase_date#278]
Condition : ((isnotnull(customer_id#275) AND NOT (customer_id#275 = UNKNOWN_CUSTOMER)) AND isnotnull(purchase_date#278))

(8) Project
Output [2]: [customer_id#275, date_trunc(month, cast(purchase_date#278 as timestamp), Some(Asia/Calcutta)) AS activity_month#288]
Input [2]: [customer_id#275, purchase_date#278]

(9) HashAggregate
Input [2]: [customer_id#275, activity_month#288]
Keys [2]: [customer_id#275, activity_month#288]
Functions: []
Aggregate Attributes: []
Results [2]: [customer_id#275, activity_month#288]

(10) Exchange
Input [2]: [customer_id#275, activity_month#288]
Arguments: hashpartitioning(customer_id#275, activity_month#288, 200), ENSURE_REQUIREMENTS, [plan_id=1353]

(11) HashAggregate
Input [2]: [customer_id#275, activity_month#288]
Keys [2]: [customer_id#275, activity_month#288]
Functions: []
Aggregate Attributes: []
Results [1]: [customer_id#275]

(12) BroadcastExchange
Input [1]: [customer_id#275]
Arguments: HashedRelationBroadcastMode(List(input[0, string, true]),false), [plan_id=1356]

(13) BroadcastHashJoin
Left keys [1]: [customer_id#262]
Right keys [1]: [customer_id#275]
Join type: Inner
Join condition: None

(14) Project
Output [2]: [cohort_month#205, customer_id#275]
Input [3]: [customer_id#262, cohort_month#205, customer_id#275]

(15) HashAggregate
Input [2]: [cohort_month#205, customer_id#275]
Keys [2]: [cohort_month#205, customer_id#275]
Functions: []
Aggregate Attributes: []
Results [2]: [cohort_month#205, customer_id#275]

(16) HashAggregate
Input [2]: [cohort_month#205, customer_id#275]
Keys [2]: [cohort_month#205, customer_id#275]
Functions: []
Aggregate Attributes: []
Results [2]: [cohort_month#205, customer_id#275]

(17) HashAggregate
Input [2]: [cohort_month#205, customer_id#275]
Keys [1]: [cohort_month#205]
Functions [1]: [partial_count(distinct customer_id#275)]
Aggregate Attributes [1]: [count(customer_id#275)#208L]
Results [2]: [cohort_month#205, count#291L]

(18) Exchange
Input [2]: [cohort_month#205, count#291L]
Arguments: hashpartitioning(cohort_month#205, 200), ENSURE_REQUIREMENTS, [plan_id=1363]

(19) HashAggregate
Input [2]: [cohort_month#205, count#291L]
Keys [1]: [cohort_month#205]
Functions [1]: [count(distinct customer_id#275)]
Aggregate Attributes [1]: [count(customer_id#275)#208L]
Results [2]: [cohort_month#205, count(customer_id#275)#208L AS active_customers#198L]

(20) AdaptiveSparkPlan
Output [2]: [cohort_month#205, active_customers#198L]
Arguments: isFinalPlan=false
```

</details>

### Why

This one is a correctness fix as much as a performance one, and it's not something the optimizer can apply on its own -- the filter is *absent* in the unoptimized version, not just misplaced, so there's no rewrite rule that invents it. Both plans happen to pick the same join strategy here (BroadcastHashJoin -- `monthly_activity` is small enough to auto-broadcast either way), so the visible EXPLAIN difference is in the Filter/PushedFilters at the two `Scan parquet` nodes and the row volume flowing into the `HashAggregate`/`Exchange` stages that build `first_purchase` and `monthly_activity` -- fewer, valid rows shuffled in the optimized plan. The bigger issue is correctness: on this dataset the two versions' row *counts* both happen to be 28, but the values differ -- the 2023-01 cohort reports 1406 active customers unoptimized vs. 1405 optimized. The `UNKNOWN_CUSTOMER` sentinel row (cleansing's fill-in for a missing customer_id) collapses every unrelated transaction with a missing customer into one fake mega-customer whose 'cohort month' is the earliest of those transactions, and who then shows up 'active' in whatever months any of them happened to land in -- a synthetic retention signal that doesn't correspond to any real person. Filtering it out (and null purchase_date rows, which can't be assigned a cohort month at all) in one shared CTE before both `first_purchase` and `monthly_activity` are built keeps that fake customer out of the result entirely, not just out of one branch of the join.

